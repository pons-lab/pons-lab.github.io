# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS / Windows
.DS_Store
Thumbs.db

# 원본 사진 보관 (빌드/배포에는 포함되지 않음 — src/assets/ 의 정리된 사본을 사용)
_originals/

# Claude Code 작업용 파일 (사이트와 무관)
.claude/
skills-lock.json

# 원재료 파일 — 사이트 데이터로 옮기기 위한 재료일 뿐, 저장소에 두지 않는다
# (CLAUDE.md 「원재료 파일 다루기」 참고)
*.docx
*.doc
*.xlsx
*.xls
*.csv
*.pptx

# 연구 카드 이미지 생성 중간 산출물 (npm run figures 가 만들었다 지움)
prep-*.png

# 사용자가 넣은 원재료 파일 (사이트 데이터로 옮긴 뒤에는 저장소에 두지 않는다)
images/

# 개인용 실적 엑셀 (내 컴퓨터에만 둔다)
_exports/
